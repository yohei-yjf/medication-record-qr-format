# 仕様の対応状況

対象仕様: **JAHIS電子版お薬手帳データフォーマット仕様書 Ver.2.6**(JAHIS技術文書 24-104 / 2024年9月)

実装は仕様書PDFの以下の箇所に基づいています。

| 仕様書の箇所 | 実装 |
| --- | --- |
| 3.1 バージョン情報 | `src/spec/version.ts` |
| 3.2.1 ファイル形式 / 3.2.3 ASCIIコード / 3.2.4 注意事項等 | `src/text/records.ts`, `src/qr/charset.ts` |
| 3.2.2 データの型 | `FieldType`(9/X/N) と `src/validate.ts` |
| 3.2.5 情報グループとレコード情報 | `src/types.ts` |
| 3.2.6 レコード出力順 | `src/serialize.ts` |
| 3.2.7 レコード出力条件 | `RecordSpec.repetition`, `FieldSpec.required` |
| 3.2.8 各種レコードレイアウト | `src/spec/records.ts` |
| 3.2.9(1) RP番号について | `Rp` / `DoctorGroup` の組み立て(`src/parse.ts`) |
| 3.2.9(3) データを分割した場合の出力方法 | `src/qr/split.ts` |
| 別表1〜4 各種コード表 | `src/spec/codes.ts` |
| 付録1 お薬手帳イメージと出力データ例 | `test/fixtures/example-01..11.txt` |

## 実装上の要点

### バージョン情報

Ver.2.6 のバージョン情報は **`JAHISTC08`** です(Ver.2.5 と同じ。Ver.2.6 の改訂は薬品コードの
備考追加のみで、バージョン情報は変更されていません)。`FORMAT_VERSIONS` に改訂履歴どおりの
対応表を持っています。

### 情報の提供方向によって必須項目が変わる

仕様書のレコードレイアウトは「医療機関等⇒患者等」と「患者等⇒医療機関等」で必須項目が異なります。
バージョンレコードの出力区分(1/2)から提供方向を判定し、その方向の必須項目のみを検証します。
出力区分が読み取れない場合は、両方向で必須の項目のみを必須として扱います。

### 用法名称の条件付き必須(表3-23)

用法名称は「薬局が出力する場合、剤形コードが『材料』『その他』以外は必須」です。
処方－医療機関レコード(51)は薬局で調剤を行った場合にのみ出力されるため、
その有無を薬局出力かどうかの判定に使っています。

### 年月日

西暦8桁(`YYYYMMDD`)と和暦7桁(`GYYMMDD`、年号は別表1の M/T/S/H/R)の両方を受け付けます。
値は変換せず、記録されたままの文字列として保持します。

### 末尾の空項目

仕様書の出力データ例は末尾の項目まで区切り文字を出力しているため、既定では省略しません。
QRコードの容量を節約したい場合は `omitTrailingEmptyFields: true` を指定してください
(読み取り側はどちらの形式も受け付けます)。

## 仕様書内で記載が一致していない箇所

付録1の出力データ例と、3.2.8のレコードレイアウトが一致しない箇所が2つあります。
本ライブラリは**レコードレイアウト(表)を正**として検証し、出力データ例はそのままフィクスチャとして
保持したうえで、検出される問題をテストの期待値にしています。

| 箇所 | 内容 | 本ライブラリの扱い |
| --- | --- | --- |
| 例7・例11 | かかりつけ薬剤師レコードの「連絡先」は表3-31では必須(◎◎)だが、出力データ例(`701,薬剤師 太郎,工業会薬局 駅前店,,,,1`)では空 | `missing-required-field` エラーとして報告(解析は継続し、他の項目は取得できる) |
| 例8 | 医療機関等提供情報レコードは表3-27では3項目だが、出力データ例(`411,…,31,,,,,,1`)は8項目 | `too-many-fields` 警告と、レコード作成者の `missing-required-field` エラーとして報告 |

実データでも同様の逸脱は起こりうるため、これらは**解析を中断せず**に報告する設計としています。

## 対象外

- QRコードのシンボル種別・型番・セルサイズなどの印字仕様(仕様書 2.1 のとおり本仕様書の対象範囲外)
- QRコードの「コード連結機能」によるシンボル分割(仕様書 3.2.9(3) のデータ分割とは別物)
- 付録2「ファイルによる連携」のフォルダ構成・ファイル名規則
- 薬品マスタ等による薬品コードの実在性チェック
